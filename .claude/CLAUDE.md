When running bash commands, prefer relative paths over absolute paths unless specifically needed.

always use 'uv run' to run python scripts in "per-option-value" folder. Must move to destinated folder and run 'uv run python' command. Must complete the former step before continue to next step.
strictly follow my bash command.
Before running any script:
1. use "pwd" to check where you are